# pytest.ini
[pytest]
minversion = 6.0
addopts = -ra -q
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Ensure root directory is the project root
# so "import app" always works
pythonpath = .
